# 贡献指南

课程、翻译、修复、输出物——所有贡献都欢迎。每个拉取请求只包含一项贡献，这样可以加快审查速度，并确保贡献者计数和学分正确。

## 重要提示：README和ROADMAP是网站的数据源

`site/build.js` 会解析 `README.md`、`ROADMAP.md` 和 `glossary/terms.md` 来生成 `site/data.js`。在修改这些文件的任何拉取请求中，以下两种模式必须保持完整：

- 阶段标题必须采用 `### 阶段N: 名称 \`X节课\`` 格式，或 `<details><summary><b>阶段N — 名称</b> ... <code>X节课</code> ... <em>描述</em></summary>` 格式
- 课程表格必须采用 `| 序号 | 课程 | 类型 | 语言 |` 的列结构（顶点项目表格用 `| 序号 | 项目 | 组合 | 语言 |`）。"语言"列接受纯文本（`Python, TypeScript`）或传统的表情符号旗帜（`🐍 🟦 🦀 🟣 ⚛️`）；两者对解析器是等价的
- ROADMAP中的状态符号（`✅`、`🚧`、`⬚`）必须保留在阶段标题和课程行上。不要用文字替换它们——解析器依赖这些精确字符

编辑这些文件后，请运行 `node site/build.js`；如果你的修改在结构上是安全的，`git diff site/data.js` 应该只显示时间戳的变化。

## 贡献方式

### 1. 添加新课程

每节课位于 `phases/XX-阶段名称/NN-课程名称/` 目录下，结构如下：

```
NN-课程名称/
├── code/           至少一个可运行的实现
├── notebook/       用于实验的Jupyter笔记本（可选）
├── docs/
│   └── en.md       课程文档（必需）
└── outputs/        本节课产出的提示词、技能或智能体（如适用）
```

**课程文档格式**（`en.md`）：

```markdown
# 课程标题

> 一句话核心理念 — 用一句话概括核心思想。

## 问题引入

为什么这很重要？没有它你做不到什么？

## 概念讲解

用图表、可视化和直觉来解释。代码放在后面。

## 从零实现

逐步从零开始实现。

## 框架应用

现在使用真实的框架或库来做同样的事情。

## 产物交付

本节课产出的提示词、技能、智能体或工具。

## 练习

1. 练习一
2. 练习二
3. 挑战练习
```

### 2. 添加翻译

在任何课程的 `docs/` 文件夹中创建新文件：

```
docs/
├── en.md    （英文 — 始终必需）
├── zh.md    （中文）
├── ja.md    （日文）
├── es.md    （西班牙文）
├── hi.md    （印地文）
└── ...
```

保持与英文版本相同的结构。翻译内容，不翻译代码。

### 3. 添加输出物

如果某节课应该产出可复用的提示词、技能、智能体或MCP服务器：

1. 在该课程的 `outputs/` 文件夹中创建它
2. 在顶层 `outputs/` 索引中添加引用

**提示词格式：**

```markdown
---
name: 提示词名称
description: 这个提示词的功能
phase: 14
lesson: 01
---

[系统提示词或模板内容]
```

**技能格式：**

```markdown
---
name: 技能名称
description: 这个技能教授什么
version: 1.0.0
phase: 14
lesson: 01
tags: [智能体, 循环]
---

[技能内容]
```

### 4. 修复Bug或改进现有课程

- 修复无法运行的代码
- 改进解释说明
- 添加更好的图表
- 更新过时信息

### 5. 添加练习或项目

更多练习和项目总是受欢迎的，特别是连接多个阶段的综合项目。

## 贡献准则

- **代码必须可运行。** 每个代码文件都应该能在列出的依赖下无错误执行。
- **代码中不加注释。** 代码应该是自解释的。解释说明放在文档中。
- **选用最合适的语言。** 不要在TypeScript或Rust更合适的地方强行使用Python。
- **先从零实现。** 在展示框架版本之前，始终先从第一性原理实现概念。
- **保持实用性。** 理论为实践服务，而非相反。
- **拒绝AI套话。** 像人一样写作。直接了当。去掉废话。

## 拉取请求流程

1. Fork本仓库
2. 创建功能分支（`git checkout -b add-lesson-phase3-gradient-descent`）
3. 进行修改
4. 确保所有代码可运行
5. 提交带有清晰描述的拉取请求

## 行为准则

参见 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。友善、乐于助人、建设性。

## 风格规范

- 直接的文风。去掉废话。匹配手册的语气，不是营销文案。
- 标题中不使用装饰性表情符号。语言列的表情旗帜是唯一例外，且仅因为解析器需要映射它们。
- 代码在列出的依赖下可直接运行。
- 先从零实现，再用框架实现。
